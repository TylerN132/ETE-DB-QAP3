<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Welcome</title>
  </head>
  <body>
    <ul>
      <% users.forEach(user=>{ %>
      <li>username: <% user.username %> userid: <% user.id %></li>
      <% }) %>
    </ul>
  </body>
</html>
